{% include "craftwarmer/craft-warmer" %}

{% js %}
var warmer_settings = {
	totalUrls: {{ totalUrls }}, 
	urlLimit: {{ urlLimit }},
	processLimit: {{ processLimit }},
	isAdmin: true,
	secret: false,
	disableLocking: {{ disableLocking ? 'true' : 'false' }}
};
{% endjs %}
